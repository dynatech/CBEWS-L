const { getDefaultConfig } = require('metro-config');

module.exports = {
  presets: ['module:@haul-bundler/babel-preset-react-native'],
  // presets: ['module:@haul-bundler/babel-preset-react-native']
};
